[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "promenade"
version = "0.1.0"
description = "AI agent for discovering cultural leisure — museums, exhibitions, concerts and events."
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
